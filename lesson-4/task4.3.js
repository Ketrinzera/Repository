let radius = 12; 
let height = 40;

let volume = Math.PI * Math.pow(radius, 2) * height; 
let roundedVolume = volume.toFixed(2);

console.log("radius:", radius);
console.log("eight:", height);
console.log("volume:", roundedVolume);